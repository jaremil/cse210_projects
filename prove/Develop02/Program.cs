using System.IO; 

class Program {

    static void Main() {
        Console.WriteLine("Hello world");
        int userInput = 0;
        if (userInput == 1) {
            
        };

        PromptGenerator promptGenerator = new PromptGenerator();
        List<string> prompt = promptGenerator._prompts;

    }
}
